<godepcop>
  <!-- The jiri tool is critical since it's used for bootstrapping the
       codebase.

       Ensure the strict rule of only allowing dependencies on github.com/dahlia-os/jiri, to
       reduce the exposure to breakage.  -->
  <pkg allow="github.com/dahlia-os/jiri/..."/>
  <pkg deny="..."/>
</godepcop>
